@page
@model asp_presentacion.Pages.Ventanas.MantenimientosModel
@{
    ViewData["Titulo"] = "Mantenimientos";
}

<div class="containers_vn">

    <form method="POST" enctype="multipart/form-data">

        @Html.AntiForgeryToken()

        <!-- =====================================================
             TÍTULO + BOTONES
             ===================================================== -->
        <div class="d-flex justify-content-between align-items-center mb-3">

            <h3 class="m-0">Mantenimientos</h3>

            <div class="btn-group">

                <input type="submit"
                       value="Refrescar"
                       asp-page-handler="BtRefrescar"
                       class="btn btn-secondary"
                       disabled="@(Model.Accion != lib_dominio.Nucleo.Enumerables.Ventanas.Listas)" />

                <input type="submit"
                       value="Nuevo"
                       asp-page-handler="BtNuevo"
                       class="btn btn-primary"
                       disabled="@(Model.Accion != lib_dominio.Nucleo.Enumerables.Ventanas.Listas)" />

                <input type="submit"
                       value="Guardar"
                       asp-page-handler="BtGuardar"
                       class="btn btn-primary"
                       disabled="@(Model.Accion == lib_dominio.Nucleo.Enumerables.Ventanas.Listas)" />

                <input type="submit"
                       value="Cancelar"
                       asp-page-handler="BtCancelar"
                       class="btn btn-secondary"
                       disabled="@(Model.Accion == lib_dominio.Nucleo.Enumerables.Ventanas.Listas)" />

            </div>

        </div>

        <!-- =====================================================
             CAMPOS OCULTOS
             ===================================================== -->
        @if (Model.Accion != lib_dominio.Nucleo.Enumerables.Ventanas.Listas)
        {
            <input type="hidden" asp-for="@Model.Actual!.Id" />
        }

        @if (Model.Accion == lib_dominio.Nucleo.Enumerables.Ventanas.Borrar)
        {
            <input type="hidden" asp-for="@Model.Actual!.Descripcion" />
            <input type="hidden" asp-for="@Model.Actual!.Costo" />
            <input type="hidden" asp-for="@Model.Actual!.Fecha" />
            <input type="hidden" asp-for="@Model.Actual!.Responsable" />
            <input type="hidden" asp-for="@Model.Actual!.Finca" />
        }

        <!-- =====================================================
             FORMULARIO EDITAR / NUEVO
             ===================================================== -->
        @if (Model.Accion == lib_dominio.Nucleo.Enumerables.Ventanas.Editar)
        {
            <div class="containers_em mt-3">

                <h4 class="mb-3">Mantenimiento</h4>

                <div class="row g-3">

                    <div class="col-md-6">
                        <label>Finca</label>
                        <select asp-for="@Model.Actual!.Finca" class="form-control">
                            <option value="">Seleccione una finca</option>
                            @foreach (var finca in Model.ListaFincas!)
                            {
                                <option value="@finca.Id">@finca.Nombre</option>
                            }
                        </select>
                    </div>

                    <div class="col-md-6">
                        <label>Descripción</label>
                        <input class="form-control"
                               asp-for="@Model.Actual!.Descripcion" />
                    </div>

                    <div class="col-md-6">
                        <label>Costo</label>
                        <input class="form-control"
                               asp-for="@Model.Actual!.Costo" />
                    </div>

                    <div class="col-md-6">
                        <label>Fecha</label>
                        <input class="form-control" type="date"
                               asp-for="@Model.Actual!.Fecha" />
                    </div>

                    <div class="col-md-6">
                        <label>Responsable</label>
                        <input class="form-control"
                               asp-for="@Model.Actual!.Responsable" />
                    </div>

                </div>

            </div>
        }

        <!-- =====================================================
             MENSAJES
             ===================================================== -->
        @if (ViewData.ContainsKey("Mensaje"))
        {
            <input type="hidden" asp-for="@Model.Accion" />
            @await Html.PartialAsync("../Emergentes/Mensajes.cshtml")
        }

        <!-- =====================================================
             POPUP BORRAR
             ===================================================== -->
        @if (Model.Accion == lib_dominio.Nucleo.Enumerables.Ventanas.Borrar)
        {
            <div class="popup">
                <div class="pp_del_inner">

                    <h3 class="text-start">Eliminar Mantenimiento</h3>

                    <p class="mt-3 text-start">¿Desea borrar este registro?</p>

                    <h5 class="text-start">
                        @Model.Actual!.Descripcion
                    </h5>

                    <div class="mt-4 text-center">
                        <input type="submit"
                               value="Borrar"
                               class="btn btn-danger me-2"
                               asp-page-handler="BtBorrar" />

                        <input type="submit"
                               value="Cancelar"
                               class="btn btn-secondary"
                               asp-page-handler="BtCancelar" />
                    </div>

                </div>
            </div>
        }

        <!-- =====================================================
             LISTADO
             ===================================================== -->
        @if (Model.Accion == lib_dominio.Nucleo.Enumerables.Ventanas.Listas ||
                Model.Accion == lib_dominio.Nucleo.Enumerables.Ventanas.Borrar)
        {
            <div class="row g-2 mb-3">

                <div class="col-md-4">
                    <label>Finca:</label>
                    <select asp-for="@Model.Filtro!.Finca" class="form-control">
                        <option value="">Todas</option>
                        @foreach (var finca in Model.ListaFincas!)
                        {
                            <option value="@finca.Id">@finca.Nombre</option>
                        }
                    </select>
                </div>

                <div class="col-md-4">
                    <label>Responsable:</label>
                    <input class="form-control"
                           asp-for="@Model.Filtro!.Responsable"
                           placeholder="Buscar por responsable" />
                </div>

                <div class="col-md-4">
                    <label>Fecha:</label>
                    <input class="form-control" type="date"
                           asp-for="@Model.Filtro!.Fecha" />
                </div>

            </div>

            <div class="containers_em">

                <table class="tb_data">

                    <thead>
                        <tr>
                            <th>Finca</th>
                            <th>Descripción</th>
                            <th>Costo</th>
                            <th>Fecha</th>
                            <th>Responsable</th>
                            <th class="cl_actions">Acciones</th>
                        </tr>
                    </thead>

                    <tbody>
                        @* 
                       Se recorre Model.Lista de forma segura:
                       - Si Model.Lista NO es null, se usa esa lista.
                       - Si Model.Lista es null, se usa Enumerable.Empty<Entidad>(), 
                         que devuelve una lista vacía del tipo indicado.
                       Así evitamos errores por null (NullReferenceException) 
                       y el foreach siempre tiene algo que iterar, aunque sea una colección vacía.
                    *@
                        @foreach (var elemento in Model.Lista ?? Enumerable.Empty<lib_dominio.Entidades.Mantenimientos>())
                        {
                            <tr>
                                <td>@elemento._Finca?.Nombre</td>
                                <td>@elemento.Descripcion</td>
                                <td>@elemento.Costo</td>
                                <td>@elemento.Fecha.ToShortDateString()</td>
                                <td>@elemento.Responsable</td>

                                <td>

                                    <input type="submit"
                                           value="Modificar"
                                           class="btn btn-light btn-sm"
                                           asp-page-handler="BtModificar"
                                           asp-route-data="@elemento.Id" />

                                    <input type="submit"
                                           value="Borrar"
                                           class="btn btn-danger btn-sm ms-1"
                                           asp-page-handler="BtBorrarVal"
                                           asp-route-data="@elemento.Id" />

                                </td>
                            </tr>
                        }

                    </tbody>

                </table>

            </div>
        }

    </form>

</div>
